# Webpack implementation
Implementing webpack to bundle TS files to smaler more effective bundles.
Typechecking
Hot-module-reloading

# Other branches
Code spliiting: Code splitting using dynamic imports and TS loader to convert TS to JS, Typechecking is done separately by a plugin which runs in parallel.

Babel loader: Babel to convert TS to JS and that JS to an even older JS which is even more widely supported